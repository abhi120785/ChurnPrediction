apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: app1
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: app1
    spec:
      containers:
        image: trialpoc.azurecr.io/mlmodelwithdocker/latest 
        ports:
        - containerPort: 80
